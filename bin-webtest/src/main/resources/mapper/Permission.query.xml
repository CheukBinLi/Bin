<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE QueryList SYSTEM "Query.dtd">
<QueryList package="Controller.entity.Permission">
	<Alias name="Controller.entity.Permission" Alias="Permission" />
	<Join name="defaultWhereList">
	<![CDATA[ 
			<#if id??>
				<#if like??>
					and A.id like '%'||:id||'%' 
				<#else>
					and A.id=:id 
				</#if>
			</#if>
			<#if status??>
				<#if like??>
					and A.status like '%'||:status||'%' 
				<#else>
					and A.status=:status 
				</#if>
			</#if>
			<#if remark??>
				<#if like??>
					and A.remark like '%'||:remark||'%' 
				<#else>
					and A.remark=:remark 
				</#if>
			</#if>
			<#if permissionName??>
				<#if like??>
					and A.permissionName like '%'||:permissionName||'%' 
				<#else>
					and A.permissionName=:permissionName 
				</#if>
			</#if>
	]]>
	</Join>
	<Query name="list" type="HQL" freemarkFormat="true" Alias="true" joinRef="defaultWhereList" joinTag="@WHERE@">
		<![CDATA[ 
			FROM Permission A 
			WHERE 
			1=1 
			@WHERE@ 
		]]>
	</Query>
	<Query name="count" type="HQL" freemarkFormat="true" Alias="true" joinRef="defaultWhereList" joinTag="@WHERE@">
		<![CDATA[ 
			SELECT COUNT(1) FROM Permission A 
			WHERE 
			1=1 
			@WHERE@ 
		]]>
	</Query>
</QueryList>